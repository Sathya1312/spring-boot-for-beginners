server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bankdb
    username: root
    password: Krithvik@143

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

